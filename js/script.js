"use strict"

document.addEventListener('DOMContantLosdet', function () {
	const form = document.getElementByild('form');
	form.addEventListener('submit', formSend);

	async function formSend(e) {
		e.preventDefault();

		let error = formValidate(form);
	}

	function formValidate(form) {
		let error = 0;
		let formReq =document.quearySelectorAll('._req');
	}

});